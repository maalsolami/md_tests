---
title: Code block
description:
permalink:
lang: en-US
publish: true
draft:
enableToc: true
tags:
aliases:
cssclasses:
socialDescription:
socialImage:
created: 2025-11-07
modified: 2025-11-07
published: 2025-11-07
---
Syntax highlight is supported with the language specified after the first set of backticks. Syntax highlighting is powered by prismjs, a list of supported languages can be found [at their site](https://prismjs.com/#supported-languages)

<pre><code>```c
int main() {
	int ch;
	while ((ch=getchar())!=EOF)
		 putchar(ch);
	 return 0;
 }
```</code></pre>

```c
int main() {
	int ch;
	while ((ch=getchar())!=EOF)
		 putchar(ch);
	 return 0;
 }
```
    
	
```md
    Text indented with a tab is formatted like this, and will also look like a code block in preview. 
```

    Text indented with a tab is formatted like this, and will also look like a code block in preview. 